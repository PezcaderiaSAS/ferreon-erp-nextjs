Codemap Update Summary - 2026-09-17 (Sesión 2: Corrección de 3 Bugs Críticos & Refinamiento)
=============================================================================================
Files Scanned: ~92
Architecture Changes:
  - Neutralizador Canónico de Fechas Calendario (`src/core/utils/fechas.ts`):
    - Creación de `parsearFechaLocal` y `formatearFechaLocal` con fijación canónica de mediodía (`12:00:00`).
    - Neutralización matemática del desfase horario UTC-5 Colombia en `@react-pdf/renderer` (`ContratoAlquilerPDF.tsx`), modales de ticket y detalle de contratos, y generador de facturas PDF.
    - Suite de 13 pruebas unitarias en `tests/unit/fechas-local.test.ts`.
  - Buscador y Filtros Rápidos en Inventario de Bodega (`src/app/bodega/page.tsx`):
    - Filtrado reactivo en memoria con latencia cero (0 ms) insensible a tildes y mayúsculas mediante `normalize("NFD")`.
    - Búsqueda multi-campo sobre Nombre, SKU y Categoría.
    - Pestañas de estado físico (`Todos`, `Disponibles`, `En Obra`, `Mantenimiento`) con contadores en tiempo real.
    - Optimización de KPIs y contadores en una sola pasada acumulativa $O(N)$ sobre el array de equipos.
    - Botón de limpieza rápida (`X`) y vista vacía amigable con restablecimiento de filtros.
  - Ergonomía en Inputs Numéricos y de Logística (`src/lib/utils.ts`):
    - Creación de `parsearNumeroErgonomico` y `formatearValorErgonomico` en `src/lib/utils.ts`.
    - Implementación de valor vacío con placeholder `0` (`value === 0 ? '' : value`) y parseo tolerante en `StepEquiposLogistica.tsx`, `AprobarCotizacionModal.tsx` y `CrearCotizacionModal.tsx`, eliminando el rebote del cero al borrar con Backspace.
    - Suite de pruebas unitarias en `tests/unit/utils-ergonomia.test.ts`.

Updated Codemaps:
  - docs/CODEMAPS/architecture.md: Registrado módulo canónico `src/core/utils/fechas.ts`, buscador de bodega y patrón de inputs ergonómicos.
  - docs/CODEMAPS/frontend.md: Mapeado buscador con filtros y KPIs en `BodegaPage`, componentes ergonómicos de fletes y utilidades.
  - docs/CODEMAPS/codemap-diff.txt: Registro de trazabilidad y métricas de calidad.

Production Build Status: 23/23 routes passing clean (Exit code 0).
Unit Tests Status: 51 test suites passed, 258 tests passed (100% green).
Linter Status: Next.js lint clean (Exit code 0).
Typecheck Status: TypeScript npx tsc clean (Exit code 0).

