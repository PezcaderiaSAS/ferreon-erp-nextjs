Codemap Analysis Report
Generated: 2026-09-09
Scanned Directory: src/, docs/
==================================================

1. ARCHITECTURE CHANGES:
- Security Perimeter (CSP 3): Sanitized `src/lib/security/csp.ts` to omit `strict-dynamic` and `nonce` from `script-src` and `script-src-elem`. This prevents W3C CSP2/3 compliant browsers (Apple WebKit, Chromium) from ignoring `unsafe-inline`, enabling unblocked streaming hydration (`self.__next_f.push`) in Next.js 14 App Router.
- Apple iOS WebKit Compatibility: Injected `worker-src 'self' blob:`, `child-src 'self' blob:`, explicit `script-src-elem`/`style-src-elem`, image blob support, and WebSockets `wss://*.supabase.co`.
- Boolean Directive Conformity: `upgrade-insecure-requests` strictly delivered as a standalone keyword with no argument value (only in production HTTPS, omitted in local dev).

2. FRONTEND & UX CHANGES:
- Dynamic Stacking Context in Modals: Added `openComboboxRowId` in `AlquilerForm.tsx` to elevate the active combobox row to `zIndex: 100` (`border-teal-500 shadow-xl ring-2 ring-teal-500/20 bg-white`). This prevents preceding rows (e.g. Row 0 `zIndex: 40`) from rendering over the active search dropdown.
- Typeahead Search Ergonomics: Refined `EquipoCombobox.tsx` upward collision threshold to `220px` (lowered from `500px`), making options naturally open downwards under the user's cursor.
- Visual Polish & 10-Item Density: Solid background `bg-white dark:bg-slate-900 shadow-2xl`, dimensions `min-w-[300px] sm:min-w-[420px]` and `max-h-[340px] sm:max-h-[380px]`, and `pb-36` bottom breathing room in `#items-scroll-area`.
- React Hook Stabilization: Wrapped `addItemRow` (`AlquilerForm.tsx`), `fetchEquipos` (`bodega/page.tsx`), and `fetchClientes` (`clientes/page.tsx`) in `useCallback` with explicit dependency arrays.
- Resilient Concurrent Hydration: `fetchAllData` in `alquileres/page.tsx` now uses `Promise.allSettled` to prevent single-endpoint delays from stalling the entire UI.

3. BUILD & DEPENDENCY VERIFICATION:
- Next.js 14.2.35 `npm run build` exits with code 0 (19/19 routes compiled, 0 errors).
- Clean Vercel production deployment pipeline.
